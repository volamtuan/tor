version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "9002:9002"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:5757
    restart: always

  # Backend Flask (Cần có Dockerfile riêng trong thư mục backend của bạn)
  # backend:
  #   build: ./backend
  #   privileged: true # Cần thiết để quản lý Tor và systemctl/service
  #   ports:
  #     - "5757:5757"
  #   volumes:
  #     - /etc/tor:/etc/tor
  #     - /var/lib/tor:/var/lib/tor
  #   restart: always
